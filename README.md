# Password Generator

A small project for generating random passwords from configurable character sets.

## Learning goals

Practice randomness, strings, user options, and basic browser interaction.

> Never use a simple client-side demo as a replacement for a secure password manager or cryptographic password-generation workflow.
