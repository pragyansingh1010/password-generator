# Password Policy

Generated passwords should favor length and character variety. Never log generated passwords or store them in project data. Keep randomness provided by the browser runtime rather than predictable sequences.