# Testing Checklist

- Generate passwords at several lengths.
- Toggle character groups and verify the generated output follows the selected rules.
- Test the minimum supported length.
- Confirm copy actions do not expose values in application logs.
