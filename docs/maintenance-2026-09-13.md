# Maintenance Notes

- Respect the requested password length.
- Keep character-set options predictable.
- Avoid exposing generated passwords in persistent logs.
- Handle unsupported length values gracefully.